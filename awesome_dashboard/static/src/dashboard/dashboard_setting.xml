<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="awesome_dashboard.DashboardSettings">
        <Dialog title="'Dashboard Item Configuration'">

            <div class="p-3">

	            <p>
		            Which cards do you want to see?
	            </p>

                <t
                    t-foreach="props.items"
                    t-as="item"
                    t-key="item.id"
                >
                    <div class="form-check mb-2">

                        <input
                            class="form-check-input"
                            type="checkbox"
                            t-model="state.selectedItems[item_index]"
                            t-att-id="'dashboard-item-' + item.id"
                        />

                        <label
                            class="form-check-label"
                            t-att-for="'dashboard-item-' + item.id">
                            <t t-esc="item.description"/>
                        </label>

                    </div>
                </t>

            </div>

            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="applyChanges">
                    Apply
                </button>

                <button class="btn btn-secondary" t-on-click="props.close">
                    Cancel
                </button>
            </t>

        </Dialog>
    </t>

</templates>